#[derive(Debug)]
struct Building {
    field: Type
}

impl Building {
    
    pub fn drive(elevator: Elevator) {
        match Elevator::direction {
            Direction::Up => expr,
            _ => expr,
        }
    }
}